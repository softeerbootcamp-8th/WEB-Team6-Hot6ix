package com.hot6ix.upbid.domain.auction.repository;

/**
 * 마감이 <b>물품 행 락을 잡기 전에</b> 미리 읽어 두는 값들. 락과 아무 상관이 없는데 지연 로딩
 * 때문에 하필 락 안에서 읽히던 것들을 여기로 모았다.
 *
 * <p>락은 줄을 세우는 장치라 한 요청이 오래 잡고 있으면 그 시간이 뒤에 선 요청 수만큼
 * 곱해진다. 마감과 입찰은 같은 물품 행을 두고 줄을 서므로, 마감이 락을 오래 쥐면 그만큼
 * 입찰이 밀리고 커넥션도 더 오래 붙잡힌다.
 *
 * <p>여기 담긴 둘은 마감 이벤트에 넣을 값일 뿐이고 경매 중에 바뀌지 않는다. 방과 상품은
 * 물품이 만들어질 때 정해진다.
 *
 * <p><b>낙찰자는 여기 없다.</b> 그건 입찰이 접수될 때마다 바뀌므로 반드시 락을 잡고 읽어야
 * 한다. 미리 읽으면 그사이 들어온 입찰이 반영되지 않은 낙찰자를 발표하게 된다.
 */
public record CloseContextProjection(
        Long roomId,
        String itemName
) {
}
