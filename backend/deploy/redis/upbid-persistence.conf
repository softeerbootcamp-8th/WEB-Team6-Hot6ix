# Upbid auction state is durable data while an auction is in progress.
appendonly yes
appendfsync always

# Failing a write is safer than silently evicting an active auction key.
maxmemory-policy noeviction

# Redis 7+ stores multi-part AOF files below this persistent data directory.
dir /var/lib/redis
appenddirname appendonlydir
